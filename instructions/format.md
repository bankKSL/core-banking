Sections covered (14 total)

1. Project Structure — Folder layout for features, components, pages
2. Naming Conventions — PascalCase, camelCase rules for files, types, hooks
3. TypeScript Interfaces — Patterns for feature types, list responses, create requests, command responses
4. Zod Schemas — Full schema pattern with z.object, .optional().nullable(), .or(z.literal("")), superRefine, and edit/partial variants
5. API Functions — List, detail, create, update patterns with currentDate() normalization
6. Query Keys & Hooks — {feature}Keys factory, useQuery with keepPreviousData, useMutation with invalidateQueries
7. Form Component Pattern — Both reusable shared form and standalone page-level form templates
8. Shared Components — ClientSearch, ProductSearch, and all UI components with import paths
9. Styling — Tailwind-only, #D32F2F primary color, dark mode, layout spacing
10. Mutation & Error Handling — UseMutation pattern with invalidation, global/local error handling
11. Barrel Exports — index.ts export pattern
12. Routes — Route registration in App.tsx
13. Dependency Checklist — Required packages and versions
14. Form Field Quick Reference — Cheatsheet for all field types (text, number, date, select, client/product search, null cleaning, edit population)
